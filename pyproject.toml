[project]
name = "ch-f"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Daler", email = "dalerdavidov@campus.technion.ac.il" }
]
requires-python = ">=3.11"
dependencies = [
    "aiosqlite>=0.22.1",
    "fastapi>=0.141.1",
    "fastapi-users[sqlalchemy]>=15.0.5",
    "imagekitio>=5.9.0",
    "python-dotenv>=1.2.3",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
ch-f = "ch_f:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
